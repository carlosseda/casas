{
  "dependencies": {
    "express": "^5",
    "http-proxy-middleware": "^2.0.6"
  },
  "scripts": {
    "dev": "npm-run-all --parallel dev:front-customer proxy",
    "dev:front-customer": "cd client/customer && npm run dev",
    "install": "npm-run-all --parallel install:front-customer",
    "install:front-customer": "cd client/customer && npm install",
    "proxy": "node ./proxy.js"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}